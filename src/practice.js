const organization=funciton(props){
    return{
        <div>
        <h1>{props.name}</h1>
        <h1>{props.tagline}</h1>
        </div>
    }
}
<organization name="Upgrad" tagline="building carriers of tomorrow"/>